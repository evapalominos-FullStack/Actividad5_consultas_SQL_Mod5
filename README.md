Actividad tabla catálogo de series Netflix.
Técnología usada BD en lenguaje SQL pgAdmin4.
Método de inserción mediante consola.
Comando a usar:
INSERT TO (), VALUES(), WHERE, ORDER BY DESC, SELECT MIN(), SELECT MAX(), SELECT ROUND(AVG()), WHERE IN, WHERE NOT IN, DELETE FROM, BETWEEN, AND, OR, %b - a%
